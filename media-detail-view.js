'use strict'

var React = require('react-native');

var {
  Image,
  ScrollView,
  Text,
  TouchableHighlight,
  View,
} = React;

var MediaDetailView = React.createClass({
  render: function() {
    return (
      <View></View>
    );
  }
});

module.exports = MediaDetailView;
